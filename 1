#include <iostream>;
#include <conio.h>;

int delete_duplicates (n, a[n])
{
    for (int i=1; i<=n; i++)
    {
        a[i]=m;
        for (int j=i=1; i<n; j++)
        {
            if (a[j]==m)
            {
                delete a[j];
            }
            else 
            {
            cout<<a[j]<<' ';
            }
        }

    }
}

using namespace std;
int main ()
{
    int n, m, a[n];
    cin>>n;
    for (int i=1; i<=n; i++)
    {
        cin>>a[i];
    }
    cout<<delete_duplicates;
    return 0;
}
